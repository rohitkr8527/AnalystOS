name: analystos
version: '1.0.0'
config-version: 2
profile: analystos
model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
seed-paths: [seeds]
models:
  analystos:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: table

