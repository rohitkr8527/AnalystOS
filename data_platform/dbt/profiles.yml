analystos:
  target: local
  outputs:
    local:
      type: postgres
      host: 127.0.0.1
      port: 5432
      user: analystos_loader
      password: "{{ env_var('WAREHOUSE_LOADER_PASSWORD') }}"
      dbname: demo_warehouse
      schema: dbt
      threads: 4
